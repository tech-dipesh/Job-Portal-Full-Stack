version: '3'
# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: backend
spec:
  selector:
    app: backend
  type: 
  ports:
  - name: backend
    protocol: 
    port: 3000
    targetPort: 3000
    nodePort: 30001
---
metadata:
  name: backend
  namespace: backend
spec:
  selector:
    app: backend
  type: 
  ports:
  - name: backend
    protocol: 
    port: 3000
    targetPort: 3000
    nodePort: 30001
metadata:
  name: backend
  namespace: backend
spec:
  selector:
    app: backend
  type: 
  ports:
  - name: backend
    protocol: 
    port: 3000
    targetPort: 3000
    nodePort: 30001
---

